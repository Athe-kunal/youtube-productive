{
  "name": "youtube-intent-filter",
  "version": "0.1.0",
  "private": true,
  "description": "Curate the YouTube feed by semantic intent, entirely on-device.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "fetch-model": "node scripts/fetch-model.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "happy-dom": "^15.0.0"
  },
  "allowScripts": {
    "esbuild@0.23.1": true,
    "protobufjs@6.11.6": true,
    "sharp@0.32.6": true
  }
}
